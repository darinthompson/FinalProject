import { FilterOrgsPipe } from './filter-orgs.pipe';

describe('FilterOrgsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrgsPipe();
    expect(pipe).toBeTruthy();
  });
});
